{
  "name": "android-intercom",
  "version": "0.1.0",
  "private": true,
  "description": "Conduit Android reference intercom — framework validation UI",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npm run build && node scripts/ensure-android.mjs && npx cap sync android",
    "cap:android": "node scripts/ensure-android.mjs && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "conduit-plugin": "file:./plugins/conduit"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
